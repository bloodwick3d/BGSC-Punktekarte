# Apps-Script-Dateien für Form-POST v5

- `Code.gs`: vollständiges Backend inklusive `doPost()`
- `Bridge.html`: direkte Statusseite der `/exec`-URL
- `appsscript.json`: Manifest mit Drive API v3

Nach Änderungen immer eine **neue Version** der bestehenden Web-App-Bereitstellung
veröffentlichen. Die `/exec`-URL bleibt dabei normalerweise gleich.
